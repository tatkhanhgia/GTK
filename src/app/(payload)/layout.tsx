import React from 'react'

// Isolated layout for Payload admin — no site chrome (nav, footer, themes)
export default function PayloadAdminLayout({ children }: { children: React.ReactNode }) {
  return children
}
