// Payload import map — populated automatically by `npx payload generate:importmap`
// Add custom component imports here as collections/plugins are added in later phases.
// See: https://payloadcms.com/docs/admin/import-map

export const importMap = {}
